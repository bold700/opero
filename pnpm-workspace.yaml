# Build-scripts van native deps expliciet uit: niet nodig voor de statische
# export (images: unoptimized) en de eslint-resolver. Zo stopt pnpm met de
# ERR_PNPM_IGNORED_BUILDS-fout in CI.
allowBuilds:
  sharp: false
  unrs-resolver: false
